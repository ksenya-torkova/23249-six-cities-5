import PropTypes from "prop-types";

const appTypes = {
  placesAmount: PropTypes.number.isRequired,
};

const mainPageTypes = {
  placesAmount: PropTypes.number.isRequired,
};

export {appTypes, mainPageTypes};
